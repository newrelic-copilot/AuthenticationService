spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
# Caps general HTTP request header size at the container level.
# Multipart part-header size is enforced in code via ServletFileUpload#setPartHeaderSizeMax.
server.max-http-request-header-size=8KB
